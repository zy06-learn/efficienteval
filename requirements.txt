# Stage 3: everything needed to reproduce every table and ablation in the paper by reading
# the frozen scores in code/experiments/00_inputs. CPU only, no model downloads.
#
# Versions are pinned, not floated. The release gate in tests/test_reproduction.py requires
# the reference arm to reproduce the frozen AUROC bit-for-bit, and the random forest's
# bootstrap and node-level feature subsampling are the only randomness in the pipeline, so a
# different scikit-learn build can move the last digits.
#
# Recorded environment: dgxspark, NVIDIA GB10, CUDA 13.0, driver 580.142, Python 3.12.

numpy==2.3.5
pandas==3.0.2
scikit-learn==1.8.0
scipy==1.17.1
pyarrow==23.0.1

# Test runner for `./reproduce.sh verify`.
pytest==9.0.1

# The frozen feature module `summary_router_compact16_direct_v1` is on the import path of
# every stage-3 entry point, and it defines a torch MLP and a pysbd segmenter at module
# level. The reference arm uses neither -- its head is the random forest above, and the
# features are read from `00_inputs` -- but both must import for the module to load, so
# they are stage-3 requirements in fact and are declared here rather than left to fail at
# `./reproduce.sh verify`. The CPU wheel is enough; no model weights are downloaded.
torch==2.11.0
pysbd==0.3.4
